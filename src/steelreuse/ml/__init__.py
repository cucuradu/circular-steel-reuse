"""ML layer: capacity surrogate (speed-only), member clustering, and reuse-potential scoring.

Per project rules, ML's authoritative role is the *non-formula* judgments (clustering, reuse score).
The capacity surrogate is a fast pre-screen only; the deterministic EN 1993 check is the truth.
"""
