<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SteelReuse - Scenario sweep" Height="600" Width="740"
        WindowStartupLocation="CenterScreen" Background="#FAFAFA">
  <Window.Resources>
    <Style TargetType="Button">
      <Setter Property="Padding" Value="10,4"/>
      <Setter Property="Margin" Value="4,0,0,0"/>
    </Style>
    <Style TargetType="Label">
      <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="Padding" Value="3,2"/>
    </Style>
  </Window.Resources>

  <Grid Margin="14">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,10" Foreground="#444"
               Text="Lock the donor + demand, pick which dials to vary, and run every combination at once. Each combination is a normal, lean match run; the trade-off board then shows which settings reuse the most and what each choice costs in the others."/>

    <!-- Donor / demand -->
    <Grid Grid.Row="1" Margin="0,0,0,6">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="110"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <Label Grid.Row="0" Grid.Column="0" Content="Donor model"/>
      <TextBox Grid.Row="0" Grid.Column="1" x:Name="donor_box" Margin="0,2"/>
      <Button Grid.Row="0" Grid.Column="2" x:Name="donor_browse" Content="Browse..."/>
      <Label Grid.Row="1" Grid.Column="0" Content="Demand model(s)"/>
      <TextBox Grid.Row="1" Grid.Column="1" x:Name="demand_box" Margin="0,2"
               ToolTip="One model, or several separated by ';' (portfolio)."/>
      <Button Grid.Row="1" Grid.Column="2" x:Name="demand_browse" Content="Browse..."/>
    </Grid>

    <Label Grid.Row="2" Content="Dials to vary" FontWeight="Bold" Margin="0,8,0,2"/>

    <!-- Axes: enable + comma-separated value list each -->
    <Grid Grid.Row="3">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="200"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <CheckBox Grid.Row="0" Grid.Column="0" x:Name="obj_check" IsChecked="True"
                VerticalAlignment="Center" Margin="0,3" Content="Objective"/>
      <TextBox  Grid.Row="0" Grid.Column="1" x:Name="obj_box" Margin="0,3" Text="co2, members, mass"
                ToolTip="Any of: co2, members, mass (comma-separated)."/>

      <CheckBox Grid.Row="1" Grid.Column="0" x:Name="minutil_check"
                VerticalAlignment="Center" Margin="0,3" Content="Min utilisation"/>
      <TextBox  Grid.Row="1" Grid.Column="1" x:Name="minutil_box" Margin="0,3" Text="0.0, 0.5, 0.7"
                ToolTip="Utilisation floor(s), comma-separated (e.g. 0.0, 0.5, 0.7)."/>

      <CheckBox Grid.Row="2" Grid.Column="0" x:Name="maxsec_check"
                VerticalAlignment="Center" Margin="0,3" Content="Max distinct sections"/>
      <TextBox  Grid.Row="2" Grid.Column="1" x:Name="maxsec_box" Margin="0,3" Text="none, 6, 10"
                ToolTip="Section-variety cap(s); 'none' = no cap (e.g. none, 6, 10)."/>

      <CheckBox Grid.Row="3" Grid.Column="0" x:Name="knock_check"
                VerticalAlignment="Center" Margin="0,3" Content="Knockdown"/>
      <TextBox  Grid.Row="3" Grid.Column="1" x:Name="knock_box" Margin="0,3" Text="0.9, 0.85, 0.8"
                ToolTip="Reclaimed-steel confidence factor(s); re-runs the EN checks (slower)."/>
    </Grid>

    <!-- Workers + grid count -->
    <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,12,0,0">
      <Label Content="Parallel runs"/>
      <TextBox x:Name="workers_box" Width="50" Margin="4,0,0,0"
               ToolTip="Engine processes to run at once (default = CPU count - 1)."/>
      <Label x:Name="count_text" Content="1 run(s)" Margin="20,0,0,0" FontWeight="Bold"/>
    </StackPanel>

    <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,10">
      <Button x:Name="run_button" Content="Run sweep" Background="#00A651" Foreground="White"
              FontWeight="Bold" Padding="16,5"/>
    </StackPanel>

    <TextBox Grid.Row="6" x:Name="progress_box" IsReadOnly="True" TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto" Background="#FFFFFF" BorderBrush="#DDD"
             FontFamily="Consolas" Text=""/>
  </Grid>
</Window>
