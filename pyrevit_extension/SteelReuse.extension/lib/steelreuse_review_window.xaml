<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SteelReuse - Donor review" Height="600" Width="900"
        WindowStartupLocation="CenterScreen" Background="#FAFAFA">
  <Window.Resources>
    <Style TargetType="Button">
      <Setter Property="Padding" Value="10,4"/>
      <Setter Property="Margin" Value="4,0,0,0"/>
    </Style>
  </Window.Resources>

  <Grid Margin="14">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TabControl Grid.Row="0" x:Name="tabs">
      <!-- Problems: members that need attention before matching -->
      <TabItem Header="Problems" x:Name="tab_problems">
        <Grid Margin="6">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <TextBlock Grid.Row="0" x:Name="problems_summary" FontSize="13" Margin="0,0,0,6"/>
          <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,6">
            <Button x:Name="problems_zoom" Content="Zoom to selected" Background="#0A6EBD"
                    Foreground="White" FontWeight="Bold"/>
            <TextBlock VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#666"
                       Text="(or double-click a row) - selects + zooms the element in the donor model"/>
          </StackPanel>
          <DataGrid Grid.Row="2" x:Name="problems_grid" AutoGenerateColumns="False" IsReadOnly="True"
                    HeadersVisibility="Column" GridLinesVisibility="Horizontal" CanUserSortColumns="True">
            <DataGrid.Columns>
              <DataGridTextColumn Header="Element id" Binding="{Binding id}" Width="90"/>
              <DataGridTextColumn Header="Role" Binding="{Binding role}" Width="70"/>
              <DataGridTextColumn Header="Raw section" Binding="{Binding raw_section}" Width="*"/>
              <DataGridTextColumn Header="Mapped" Binding="{Binding mapped}" Width="120"/>
              <DataGridTextColumn Header="Method" Binding="{Binding method}" Width="90"/>
              <DataGridTextColumn Header="Severity" Binding="{Binding severity}" Width="70"/>
              <DataGridTextColumn Header="Issues" Binding="{Binding issues}" Width="2*"/>
            </DataGrid.Columns>
          </DataGrid>
        </Grid>
      </TabItem>

      <!-- PDA QA: the pre-demolition audit, all members -->
      <TabItem Header="PDA QA" x:Name="tab_pda">
        <Grid Margin="6">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <TextBlock Grid.Row="0" x:Name="pda_summary" FontSize="13" Margin="0,0,0,6"/>
          <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,6">
            <Button x:Name="pda_zoom" Content="Zoom to selected" Background="#0A6EBD"
                    Foreground="White" FontWeight="Bold"/>
            <TextBlock VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#666"
                       Text="(or double-click a row)"/>
          </StackPanel>
          <DataGrid Grid.Row="2" x:Name="pda_grid" AutoGenerateColumns="False" IsReadOnly="True"
                    HeadersVisibility="Column" GridLinesVisibility="Horizontal" CanUserSortColumns="True">
            <DataGrid.Columns>
              <DataGridTextColumn Header="Element id" Binding="{Binding id}" Width="90"/>
              <DataGridTextColumn Header="Role" Binding="{Binding role}" Width="70"/>
              <DataGridTextColumn Header="Condition" Binding="{Binding condition}" Width="90"/>
              <DataGridTextColumn Header="Verification" Binding="{Binding verification}" Width="120"/>
              <DataGridTextColumn Header="Knockdown" Binding="{Binding knockdown}" Width="85"/>
              <DataGridTextColumn Header="Admitted" Binding="{Binding admitted}" Width="75"/>
              <DataGridTextColumn Header="Defects" Binding="{Binding defects}" Width="*"/>
            </DataGrid.Columns>
          </DataGrid>
        </Grid>
      </TabItem>
    </TabControl>

    <DockPanel Grid.Row="1" Margin="0,10,0,0">
      <TextBlock x:Name="hint_text" VerticalAlignment="Center" Foreground="#666"
                 Text="Donor extraction review. Use Highlight Problems to colour the whole view."/>
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button x:Name="open_report_button" Content="Open report"/>
        <Button x:Name="open_folder_button" Content="Open folder"/>
      </StackPanel>
    </DockPanel>
  </Grid>
</Window>
