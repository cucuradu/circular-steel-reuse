title: |-
  Apply to
  Demand
tooltip: >-
  This open model is the DEMAND (new design). Colour its slots by fill status (green = filled by a
  donor, amber = partially filled, orange = unfilled) and write the reuse passport parameters from a
  SteelReuse run you pick.
